sudo: required

language: python

services:
  - docker

env:
  - LIBRARY=annoy
  - LIBRARY=faiss
  - LIBRARY=falconn
  - LIBRARY=kgraph
  - LIBRARY=rpforest

before_install:
  - bash install.sh

script:
  - echo TODO
