FROM ann-benchmarks

RUN sudo apt-get install -y python-setuptools python-pip
RUN sudo pip3 install pybind11 numpy setuptools
RUN git clone https://github.com/nmslib/hnsw.git;cd hnsw; git checkout denorm

RUN cd hnsw/python_bindings; sudo python3 setup.py install

RUN python3 -c 'import hnswlib'

